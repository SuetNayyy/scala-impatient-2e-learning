// 可以将多个值变量放在一起声明
val xmax, ymax = 100  // xmax: Int = 100 ymax: Int = 100
var greeting, message: String = null  // greeting: String = null message: String = null

// Scala有七种数值类型：Byte、Char、Short、Int、Long、Float、Double和Boolean
// 这些类型是类，并且可以对数字执行方法

// Scala并没有提供 ++ 和 -- 操作符，我们需要使用 +=1 或者 -=1

// 如果一个无参方法并不修改对象，调用时就不用写括号

// 使用伴生对象的apply方法是Scala中构建对象的常用手法

// Range数字区间  Seq[Char]字符序列
